<%= form_for(@folder_attachment) do |f| %>
  <% if @folder_attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@folder_attachment.errors.count, "error") %> prohibited this folder_attachment from being saved:</h2>

      <ul>
      <% @folder_attachment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :owner_id %><br>
    <%= f.text_field :owner_id %>
  </div>
  <div class="field">
    <%= f.label :client_id %><br>
    <%= f.text_field :client_id %>
  </div>
  <div class="field">
    <%= f.label :name_file %><br>
    <%= f.text_field :name_file %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
