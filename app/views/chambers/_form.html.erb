<%= form_for(@chamber) do |f| %>
  <% if @chamber.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@chamber.errors.count, "error") %> prohibited this chamber from being saved:</h2>

      <ul>
      <% @chamber.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :building_id %><br>
    <%= f.text_field :building_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :type_chamber %><br>
    <%= f.text_field :type_chamber %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
